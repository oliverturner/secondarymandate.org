---
import Nav from "../components/nav.svelte"
// You can import components from any supported Framework here!


// Component Script:
// You can write any JavaScript/TypeScript that you'd like here.
// It will run during the build, but never in the browser.
// All variables are available to use in the HTML template below.
const {title} = Astro.props;

// Full Astro Component Syntax:
// https://docs.astro.build/core-concepts/astro-components/
---
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width">
  <title>{title}</title>

  <link rel="icon" type="image/x-icon" href="/favicon.ico" />

  <link rel="stylesheet" href="/style/global.css">
</head>
<body class="app">
  <div class="app__nav">
    <Nav />
  </div>

  <main class="app__content">
    <header class="header">
      <img class="header__image" src="/images/ballot_box.jpg" alt="A ballot box" width="80" height="127">

      <h3 class="header__title">The Secondary Mandate</h3>
      <p class="header__strapline">A viable method of democratic Lords reform</p>
    </header>
    <slot />
  </main>
</body>
</html>

<style lang="scss">
  .app__nav {
    @media (min-width: 768px) {
      position: sticky;
      top: 0;
    }
  }

  .header {
    display: grid;
    grid-template-columns: auto 1fr;
    grid-template-rows: auto auto;
    grid-template-areas:
      "image title"
      "image strapline";
    gap: 1rem;
  }

  .header__image {
    grid-area: image;

    width: 100%;
    height: auto;
  }

  .header__title {
    grid-area: title;
    align-self: end;

    margin: 0;
    font-size: 1.5rem;
  }

  .header__strapline {
    grid-area: strapline;

    margin: 0;
    font-size: 1.15rem;
  }
</style>
