<div class="navigation">
  <a href="/" title="Home" class="logo">
    <img
      srcset="/images/houseoflords-1x.jpg 230w,
              /images/houseoflords-2x.jpg 460w"
      src="/images/houseoflords-1x.jpg"
      alt="The House of Lords"
      width="230"
      height="230"
    />
  </a>
  <nav class="links">
    <a href="/how/">How it works</a>
    <a href="/about/">About This Campaign</a>
    <a href="/faqs/">Frequently Asked Questions</a>
    <a href="/links/">News & Links</a>
    <a href="mailto:billybragg@secondarymandate.org" target="_blank">Contact Us</a>
  </nav>

  <button class="navigation__menubtn" data-component="menubtn">Menu</button>
</div>

<style lang="scss">
  .navigation {
    --tint-border: transparent;
    --translate: -100%;

    display: grid;
    grid-template-rows: 230px 1fr;
    align-items: start;
    gap: 1rem;

    position: fixed;
    top: 0;
    left: 0;
    width: 230px;
    height: 100%;
    background: white;

    transform: translateX(var(--translate));
    transition: transform 0.5s ease-in-out;

    :global(.menu-open) & {
      --tint-border: #fff;
      --translate: 0;

      filter: drop-shadow(0.2rem 0 0.5rem #000a);
    }

    @media (min-width: 768px) {
      --translate: 0;
      position: static;

      :global(.menu-open) & {
        filter: none;
      }
    }
  }

  .navigation__menubtn {
    position: absolute;
    top: 0;
    left: 100%;
    padding: 0.5rem;
    border: none;
    border-radius: 0 0 0.5rem 0;
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    cursor: pointer;
    background: var(--tint-primary);
    color: white;

    :global(.menu-open) & {
      border-right:  1px solid var(--tint-border);
      border-bottom: 1px solid var(--tint-border);
    }

    @media (min-width: 960px) {
      display: none;
    }
  }

  .logo img {
    display: block;
    width: 100%;
    aspect-ratio: 1;

    @media (min-width: 768px) {
      width: 232px;
    }
  }

  .links {
    display: grid;

    padding: 1rem;

    @media (min-width: 768px) {
      padding: 0;
    }

    & a {
      --wh: 0.6rem;

      display: flex;
      align-items: center;
      gap: 0.5rem;

      padding: 0.5rem 0;
      text-decoration: none;

      &:focus,
      &:hover {
        color: var(--tint-primary);
      }

      &::before {
        content: "";
        width: var(--wh);
        height: var(--wh);
        border-radius: var(--wh);
        background-color: currentColor;
      }
    }
  }
</style>
